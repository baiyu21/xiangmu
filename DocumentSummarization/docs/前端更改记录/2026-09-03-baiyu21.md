# 前端变更记录

> 本文档用于记录所有前端代码变更，确保代码修改可追溯、可审查。

---

## 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-03 |
| **变更人** | baiyu21 |
| **需求描述** | 文件映射页去掉模块/仓库层级树；左侧文件列表点选，右侧展示修改历史简略版；点击某次修改进入详情完整内容 |
| **关联需求** | `docs/superpowers/specs/2026-09-03-file-map-list-history-layout.md` |

---

## 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/views/ProjectManagement/ProjectOverview.vue` | 模块/仓库模式切换、`FileMapTree`、scope 范围筛选与相关状态（mode/scope/collapsed） | 去除板块层级，改为左列表右简略历史 |

**删除代码详情：**

```diff
- import { PageState, FileMapTree } from '@/components'
- const mode = ref<MapMode>('module')
- const scope = ref('all')
- // treeNodes / onSelectNode / toggleCollapse / 模块·仓库切换 UI / FileMapTree
```

---

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/views/ProjectManagement/ProjectOverview.vue` | 重写 | 左文件列表（点选高亮）+ 右修改历史简略版；点击条目带 `?doc=` 进详情 |
| 2 | `src/views/ProjectManagement/ProjectFileDetail.vue` | 修改 | 支持 `route.query.doc` 预选中对应修改记录 |
| 3 | `src/views/ProjectManagement/ProjectManagement.vue` | 修改 | 文案同步新主路径 |

**新增代码详情：**

```diff
+ // Overview：选中文件 → briefHistory；点击条目
+ openDocDetail(docId) → project-file + query.doc
+
+ // FileDetail：resolveInitialDocId 优先匹配 query.doc
```

---

## 变更说明

### 变更原因
层级树增加浏览成本；希望在映射页直接预览修改历史，详情页再看 AI 简介与客户注释等完整信息。

### 影响范围
文件映射页布局与交互；文档详情进入时的默认选中项。`FileMapTree` 组件文件保留未删。

### 注意事项
- 搜索/排序仍作用于左侧文件列表
- `lint` / `test`（17）/ `build` 已通过

---

## 签名确认

- [x] 代码已自测通过
- [ ] 已完成Code Review
- [ ] 变更内容已同步至相关人员

**记录人：** baiyu21  
**记录时间：** 2026-09-03
