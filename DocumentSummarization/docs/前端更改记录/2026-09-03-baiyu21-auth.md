# 前端变更记录

> 本文档用于记录所有前端代码变更，确保代码修改可追溯、可审查。

---

## 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-03 |
| **变更人** | baiyu21 |
| **需求描述** | 对接真实认证接口：登录 `/api/v1/auth/login`、注册 `/api/v1/auth/register`、发送验证码 `/api/v1/auth/send-code` |
| **关联需求** | 后端 Auth 接口联调 |

---

## 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/views/LoginView.vue` | mock `setSession(mock-token-*)`、Demo 账号提示 | 改为真实 API |
| 2 | `e2e/vue.spec.ts` | mock 登录进个人中心用例 | 依赖后端账号，改为仅校验登录表单 UI |

---

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/api/modules/auth.ts` | 新增 | login / register / sendRegisterCode + normalizeAuthSession |
| 2 | `src/api/request.ts` | 修改 | 支持 `{code,msg,data}` 业务码解包；登录页 401 不强制跳转循环 |
| 3 | `src/views/LoginView.vue` | 修改 | 注册增加显示名、验证码与倒计时；登录/注册走 API |
| 4 | `vite.config.ts` | 修改 | `/api` → `http://127.0.0.1:8000` 代理 |
| 5 | `.env.example` / `.env.development` | 修改/新增 | `VITE_API_BASE_URL=/api` |

---

## 变更说明

### 变更原因
一期需对接后端认证，替换本地 mock 登录。

### 影响范围
登录/注册页、会话写入、本地开发代理。

### 注意事项
- 请求路径：`POST /api/v1/auth/login|register|send-code`
- 注册 body：`username, name, email, code, password`
- 登录成功需后端返回 token（兼容 `token` / `access_token`）
- `lint` / `test`(20) / `build` 已通过

---

## 签名确认

- [x] 代码已自测通过
- [ ] 已完成Code Review
- [ ] 变更内容已同步至相关人员

**记录人：** baiyu21  
**记录时间：** 2026-09-03
