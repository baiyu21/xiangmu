# 前端变更记录

> 本文档用于记录所有前端代码变更，确保代码修改可追溯、可审查。  
> **变更工程**：`xiangmu/DocumentSummarization`（文档智能摘要 / doc-sum）

---

## 变更记录 1：客户注释路径参数纠正

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | baiyu21 |
| **需求描述** | 对照接口文档，注释路径参数改为 `changeItemId`（`records[].id`），不再误传 `docId` / 变更文档 ID |
| **关联需求** | 客户注释联调纠偏 |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/views/ProjectManagement/ProjectFileDetail.vue` | 以 `selectedDoc.docId` 调注释接口的逻辑与相关文案 | 与文档 `changeItemId` 不一致 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/api/modules/comments.ts` | 修改 | 注释改为路径参数 `changeItemId` |
| 2 | `src/views/ProjectManagement/ProjectFileDetail.vue` | 修改 | 使用选中历史条 `id`（change_items 主键）拉/发注释 |

**关键接口：**

| 方法 | 路径 | Body | 用途 |
|------|------|------|------|
| GET | `/api/v1/records/{changeItemId}/comments` | — | 获取留言 |
| POST | `/api/v1/records/{changeItemId}/comments` | `{ content }` | 发表留言 |

**注意事项：**
- `changeItemId` = `file-detail` 的 `records[].id`（示例 `101`）
- `records[].docId` 仅用于跳转修改文档正文，不参与注释接口

---

## 变更记录 2：去掉修改文档正文「下载 .md」

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | baiyu21 |
| **需求描述** | 修改文档正文页移除「下载 .md」按钮及相关下载逻辑 |
| **关联需求** | 产品交互调整 |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/views/ProjectManagement/ProjectChangeRecord.vue` | 「下载 .md」按钮与 `downloadMarkdown` | 产品不再需要本地下载 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| — | — | — | 无新增，仅删除下载能力 |

---

## 变更记录 3：文档详情 · 变更类型筛选

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | baiyu21 |
| **需求描述** | 文档详情页左侧修改历史增加单选筛选：全部 / 新增 / 修改 / 删除 |
| **关联需求** | 变更类型筛选 |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| — | — | 无大段删除 | 以扩展为主 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/utils/fileMap.ts` | 修改 | `ChangeDoc.typeCode`；`resolveChangeTypeCode` / `changeTypeLabel` |
| 2 | `src/api/modules/projects.ts` | 修改 | `normalizeChangeDoc` 拆分 `type` / `typeCode` |
| 3 | `src/views/ProjectManagement/ProjectFileDetail.vue` | 修改 | 标题行下拉筛选；列表类型标签；滤空联动选中 |
| 4 | `src/utils/__tests__/fileMap.spec.ts` | 修改 | 类型编码规范化单测 |
| 5 | `docs/superpowers/specs/2026-09-04-file-detail-type-filter-design.md` | 新增 | 筛选设计说明 |

**交互要点：**
- 默认「全部类型」；按规范化后的 `typeCode`（兼容中文「新增/修改/删除」）过滤
- 当前选中被滤掉时自动选筛选结果第一条；无结果则右侧空态

---

## 变更记录 4：用户管理 · 删除操作

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | baiyu21 |
| **需求描述** | 用户管理操作列增加删除；二次确认后调用批量删除接口 |
| **关联需求** | 用户管理联调 |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| — | — | 无 | 以新增能力为主 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/api/modules/users.ts` | 新增 | `deleteUsersBatch(userIds)` → `DELETE /v1/users/batch` |
| 2 | `src/api/index.ts` | 修改 | 导出 `deleteUsersBatch` |
| 3 | `src/views/UserManagement/UserManagement.vue` | 修改 | 「删除」按钮 + `ElMessageBox` 确认；成功后刷新列表 |

**关键接口：**

| 方法 | 路径 | Body | 用途 |
|------|------|------|------|
| DELETE | `/api/v1/users/batch` | `{ user_ids: number[] }` | 批量删除用户（单删传一个 id） |

**注意事项：**
- 接口文档原未收录该接口，以前端联调约定为准
- 删除不可恢复，需二次确认

---

## 变更记录 5：修改文档正文 · 变更类型筛选

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | baiyu21 |
| **需求描述** | 查看 md 修改文档正文页，变更条目区增加与文档详情一致的类型筛选下拉 |
| **关联需求** | 变更类型筛选 |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| — | — | 无 | 以扩展为主 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/views/ProjectManagement/ProjectChangeRecord.vue` | 修改 | 「变更条目」标题旁下拉；`filteredItems` 过滤展示 |
| 2 | `src/api/modules/projects.ts` | 修改 | `normalizeChangeDocItem` 规范化 `typeCode` |

**交互要点：**
- 选项：全部类型 / 新增 / 修改 / 删除
- 标题展示筛选后数量；非「全部」时显示 `当前/总数`
- 重新加载文档时重置为「全部类型」

---

## 当日汇总

| 模块 | 状态 |
|------|------|
| 客户注释路径参数 | ✅ 使用 `changeItemId`（`records[].id`） |
| 修改文档正文去掉下载 md | ✅ |
| 文档详情类型筛选 | ✅ |
| 用户管理删除 | ✅ `DELETE /users/batch` |
| 修改文档正文类型筛选 | ✅ |

**工程路径：** `xiangmu/DocumentSummarization`  
**API 基址（联调）：** `http://139.155.154.172:5554/api`

---

## 签名确认

- [x] 代码已自测通过（相关单测已跑通关键模块）
- [ ] 已完成 Code Review
- [ ] 变更内容已同步至相关人员

**记录人：** baiyu21  
**记录时间：** 2026-09-04
