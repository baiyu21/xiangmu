# 前端变更记录

> 本文档用于记录所有前端代码变更，确保代码修改可追溯、可审查。  
> **变更工程**：`xiangmu/DocumentSummarization`（文档智能摘要 / doc-sum）  
> **分支**：`hsh`（基于 develop 集成）

---

## 变更记录 1：根路径重定向修复

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | HE343483 |
| **需求描述** | 根路径 `/` 直接重定向到登录页，避免未登录用户先跳 `/app` 再被鉴权守卫二次重定向 |
| **关联需求** | 登录入口跳转修复 |
| **提交** | `e42655c7`（fix） |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/router/index.ts` | `redirect: '/app'` | 未登录访问根路径会先进 `/app` 触发鉴权守卫再回登录页，多一次跳转 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/router/index.ts` | 修改 | 根路径 `redirect: '/login'` 并补 `meta: { public: true }`，与 `/login` 路由保持 public 语义一致 |

### 变更说明

- 改动后：访问 `/` → 直接 `/login`；已登录用户命中 `public && isAuthenticated` 守卫 → 跳 `personal`，行为与登录页一致。
- 仅路由配置变更，不影响其它模块。

---

## 变更记录 2：合并 main 到 hsh

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | HE343483 |
| **需求描述** | 将 `main` 分支合并进 `hsh`，同步 2026-09-03 的项目管理 / 文件映射 / comments 模块代码 |
| **关联需求** | 分支集成 |
| **提交** | `a0b66dac`（Merge branch 'main' into hsh） |

### 变更范围

本次为分支合并，引入内容为 2026-09-03 baiyu21 提交到 `main` 的工作，已在 `2026-09-03-baiyu21.md` 逐条记录，此处不重复登记，仅列出受影响文件清单：

| 序号 | 文件路径 | 说明 |
|------|----------|------|
| 1 | `src/api/modules/projects.ts` | 项目/文件/修改历史/变更文档接口 |
| 2 | `src/api/modules/comments.ts` | comments 模块 |
| 3 | `src/api/modules/auth.ts` | profile / 改密发码 |
| 4 | `src/api/index.ts` | 导出聚合 |
| 5 | `src/stores/fileMap.ts` | 文件映射 store |
| 6 | `src/utils/fileMap.ts` | MappedFile 计数工具 |
| 7 | `src/views/ProjectManagement/*.vue` | 项目管理/概览/文件详情/变更记录 |
| 8 | `src/api/__tests__/*.spec.ts`、`src/utils/__tests__/fileMap.spec.ts` | 单元测试 |
| 9 | `docs/change-logs/2026-09-03-baiyu21.md` | 09-03 变更记录 |

### 变更说明

- 合并未修改业务逻辑，仅同步 `main` 已有产出；详细变更见 `2026-09-03-baiyu21.md`。
- 合并后本地 `hsh` 与 `main` 在项目管理/文件映射模块达成一致。

---

## 变更记录 3：个人中心表单调整（未提交）

### 基本信息

| 字段 | 内容 |
|------|------|
| **变更日期** | 2026-09-04 |
| **变更人** | HE343483 |
| **需求描述** | 用户名不可编辑；移除"删除 Token"能力；基本信息表单项顺序调整 |
| **关联需求** | 个人中心表单调整 |
| **状态** | ⚠️ 工作区未提交（modified） |

### 变更范围

### 一、删除代码记录

| 序号 | 文件路径 | 删除范围 | 删除原因 |
|------|----------|----------|----------|
| 1 | `src/views/PersonalCenter/PersonalCenter.vue` | `deleteToken` 函数 | 产品移除"删除 Token"能力，避免用空字符串覆盖后端令牌 |
| 2 | `src/views/PersonalCenter/PersonalCenter.vue` | "删除 Token" `el-button` | 同上，入口一并下线 |

### 二、新增代码记录

| 序号 | 文件路径 | 新增类型 | 说明 |
|------|----------|----------|------|
| 1 | `src/views/PersonalCenter/PersonalCenter.vue` | 修改 | 用户名 `el-input` 加 `disabled`，作为唯一键不可编辑 |
| 2 | `src/views/PersonalCenter/PersonalCenter.vue` | 修改 | 表单项顺序调整：用户名 → 角色 → 显示名 → 邮箱（原：用户名 → 显示名 → 邮箱 → 角色） |

### 变更说明

- 用户名改为只读展示，避免修改后端唯一标识造成数据错位。
- Token 区仅保留"更新 Token"，不再提供删除入口。
- 表单项顺序按业务重要性重排，角色前置。

---

## 当日汇总

| 模块 | 状态 |
|------|------|
| 根路径重定向 `/login` | ✅ 已提交 `e42655c7` |
| 合并 main → hsh | ✅ 已提交 `a0b66dac` |
| 个人中心：用户名只读 / 移除删 Token / 表单顺序 | ⚠️ 未提交 |

**工程路径：** `xiangmu/DocumentSummarization`  
**API 基址（联调）：** `http://139.155.154.172:5554/api`

---

## 签名确认

- [x] 代码已自测通过（router 改动本地验证跳转正常）
- [ ] 个人中心改动尚未提交，待自测后 commit
- [ ] 已完成Code Review
- [ ] 变更内容已同步至相关人员

**记录人：** HE343483  
**记录时间：** 2026-09-04
